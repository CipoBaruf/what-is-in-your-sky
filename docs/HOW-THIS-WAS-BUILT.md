# How this was built

This app was written in Claude Code sessions, one per task. The MVP — tasks R1 to R15
and H — was built **by hand**: an interactive session per task, on one branch after
another, before the driver existed. Every task from **R16** on was run by a script in this
repository, each session headless in its own git worktree, with one exception: R38, a
spike the owner ran on a phone (`Model: interactive` in its entry). Nothing here is a
prompt log: the interesting part is the paperwork the sessions were given and the
evidence they left behind, and both are in the tree. This page is the reading order.

Every count on this page lives in one table at the end, [By the numbers](#by-the-numbers),
beside the command that produces it and the commit it was read at, so a post and the tree
quote one source and a stale figure has one place to be wrong.

Read it in this order and the workflow explains itself in about twenty minutes.

## 1. [SPEC.md](../SPEC.md) — what the product does

Numbered requirements (`FR-PASS-3`, `FR-LIVE-6`), user stories with acceptance criteria,
a findings table (§4.20, `F-` rows), and a Decision Log (§12, `V-` rows) that is fixed
once written. **Written by hand**, with the `sdd-spec` skill below, in a conversation with
the owner: the owner answers scope questions one at a time and the session writes the
requirement. Every `FR-` id here is the thing a later session's tests have to point at,
so a change to the product starts on this page and nowhere else. Its §1 and §5 still
carry the wording of the first proposal, with a dated *as built* note on each (V22-7),
because the history of what was proposed is part of the record.

## 2. [PLAN.md](../PLAN.md) — how it is built

Architecture, the data model, the Web Worker message contract, the testing strategy, the
bundle budgets, and §16 — how the tasks are delivered. **Written by hand** in the same
way, one section per question the spec left to engineering. Its Decision Log (the `D-`
rows, from `D-1` up; the highest is in the table below) is the other half of the
paperwork: a decision records the question, the answer and what was given up, so a later
session that disagrees has to argue with a written reason rather than with a silence.

The section to read is [§16, Delivery](../PLAN.md#16-delivery-of-the-v1-and-v11-tasks-v1-11-v11-9).
It opens with the v1 contract — lanes (which files a task may touch), waves (which tasks
can run at once), what an entry must carry, the driver, the merge policy and the model
policy — and then carries one delivery section per phase, §16.9 to §16.18, v1.2 through
v2.2, each with its wave table and the block of decision numbers its tasks may spend.

## 3. [TASKS.md](../TASKS.md) — the ordered slices

One entry per task: a goal, the requirement ids it satisfies, its lane, its dependencies,
its scope, a *done when* list, and — once it is merged — a **Done** note. **Generated by a
session** from the two documents above with the `sdd-breakdown` skill, and never edited
by hand except to check a task off. That sentence is true of hands and not of sessions:
the **Done** notes are written by the implementing session at the end of its own run.
They are where a task says what it did differently from what it was asked, and they are
the most honest thing in the repository.

## 4. [scripts/sdd-run.ts](../scripts/sdd-run.ts) — the driver

One file plus [scripts/sdd/](../scripts/sdd/) beside it. **Written outside the task
loop**, first committed on `main` on 2026-09-03 (`6b79db1`), after the MVP had shipped
and before the first v1 wave, from PLAN §16.4; then amended by the decisions it ran into:
`D-89` (a headless session commits as it goes, because a session that commits once at
the end turns any limit into total loss), `D-131` (a per-task model override), `D-132`
(waves run concurrently, capped at three, one task per lane), `D-198` (the brief). It
reads `TASKS.md`, works out which tasks are ready, and for each one:

1. makes a worktree and a branch off `origin/main`, installs the dependencies;
2. writes `sdd-run/<id>.brief.md` — the task's entry plus every requirement, story,
   finding and decision that entry names, so the implementing session never reads the
   three documents in full;
3. runs a headless Claude Code session on the brief, with a tool allowlist that cannot
   push and cannot call `gh`;
4. checks the branch has commits, rebases, pushes, opens a pull request and waits for CI;
5. runs a **second** session — a different context, sometimes a different model — whose
   only job is to review the diff and write its findings to a JSON file, which the driver
   posts on the pull request;
6. merges when the task's gate says `auto`, and stops for the owner when it says `owner`.

`npm run sdd -- --status` prints where every task stands. The driver is why the sessions
run in parallel without stepping on each other, and the lanes in PLAN §16.1 are why two
sessions editing at the same time do not collide.

## 5. One task, end to end: R28

The clearest way to see the loop is to follow a single task through it.

- **The entry:** [R28 in TASKS.md](../TASKS.md) — *Install hint, update banner and the
  favourites list*. Goal, `FR-OFF-6` and `FR-OFF-1`, three dependencies, a scope naming the
  components, a *done when* list of four items, and the lane crossings declared up front.
  Written by the breakdown session.
- **The branch and the pull request:**
  [#54](https://github.com/CipoBaruf/what-is-in-your-sky/pull/54), opened by the driver
  from what the implementing session committed. Its body is the summary that session wrote
  to `sdd-run/R28.summary.md`.
- **The review session's findings:** posted on that pull request as a comment headed
  *Automated review (PLAN §16.4)* —
  [the conversation](https://github.com/CipoBaruf/what-is-in-your-sky/pull/54). It found
  five things, which became `F-29` … `F-33` in
  [the findings table in SPEC §4.20](../SPEC.md): a saved place that does not reseed
  the coordinates field, an update offer still reachable under an open pass, an install
  listener that only exists while the shell is mounted, an uncaught `prompt()` rejection,
  and an e2e assertion that ran before hydration and passed without the guard it was
  testing. None of them were fixed on that branch. Findings become their own later task —
  `R49` was one — so the merge is not held open while a session argues with its reviewer.
- **The captures:** the session's own evidence, at 390 px in both languages —
  [the favourites list](screenshots/r28-favourites-390-dark-en.png),
  [the same list in Spanish](screenshots/r28-favourites-390-dark-es.png),
  [the install hint](screenshots/r28-install-390-dark-en.png),
  [its iOS branch](screenshots/r28-install-ios-390-dark-en.png),
  [the update banner](screenshots/r28-update-390-dark-en.png).
- **The Done note:** written back into the R28 entry when it merged. Read the last
  paragraph of it: two of the things it records were caught by the tests rather than by
  the review, and it says which.

## 6. [The four skills](../.claude/skills/) — what the sessions were told

Small Markdown files, **written by hand**, that a session loads by name. They are the
whole of the process instruction; there is no hidden prompt.

- [`sdd-spec`](../.claude/skills/sdd-spec/SKILL.md) — revise SPEC.md and then PLAN.md for a
  new phase or a scope change. Asks the owner one question at a time.
- [`sdd-breakdown`](../.claude/skills/sdd-breakdown/SKILL.md) — regenerate TASKS.md as
  vertical slices, with what each entry must carry.
- [`sdd-implement`](../.claude/skills/sdd-implement/SKILL.md) — the task loop: read the
  brief, branch, test each acceptance criterion, commit in small steps, check the task off,
  write the summary.
- [`visual-review`](../.claude/skills/visual-review/SKILL.md) — capture a task's screens at
  390 px and 1280 px, in both languages and both themes, and file them under
  `docs/screenshots/`.

## What is deliberately public

The process paperwork is in the tree on purpose, because the process is the thing this
repository shows: [`.claude/settings.json`](../.claude/settings.json) (one allow rule,
`gh pr merge`, for the driver's merge step and nothing else), the four skills above,
[`CLAUDE.md`](../CLAUDE.md) (the few lines every session starts with) and
[`scripts/sdd/`](../scripts/sdd/) (the driver's parts: the brief, the session, the waves,
the git and `gh` calls, the report). No tracked file names a path on the owner's machine
or a conversation of theirs; [`tests/docs/public.test.ts`](../tests/docs/public.test.ts)
scans every tracked text file, `.claude/**` included, for both shapes and fails on either.

What is not tracked, and why: `sdd-run/` (the briefs and summaries the driver writes for
each run, regenerated every time), `logs/` (the session logs), `.sdd-cache/` (the driver's
scratch), `handoff/` and `redesign/` (the design phase's artboards and guides, the owner's
working material, V20-11) and `promo/` (the recordings a post is cut from, FR-SHOW-6).
[`.gitignore`](../.gitignore) names them, and
[`tests/docs/hygiene.test.ts`](../tests/docs/hygiene.test.ts) asserts that nothing under
any of them is tracked.

## Why `docs/` is as big as it is

Most of it is [`docs/screenshots/`](screenshots/): the sizes and counts are in the table
below. They are not decoration. Each phase's definition of done requires a capture set
shot from the release build in both languages and both themes (`FR-COMP-6`, `D-179`) —
the `v1-*` files — and [`tests/docs/captures.test.ts`](../tests/docs/captures.test.ts)
fails if a file the set names is missing or a file it does not name has appeared. The
rest of the directory are the per-task captures each pull request carried. That is the
acceptance evidence for every screen in the app, held in the repository at the commit it
was shot from, which is the only place it can be checked against later. The remainder is
[`docs/mockups/`](mockups/) (the two owner-approved references), three spike folders —
[`dome-composition/`](dome-composition/), [`spike-glyphcss/`](spike-glyphcss/) and
[`window/`](window/), each with its findings, rasters and measurements — and
[`docs/readme/`](readme/), the two composed pictures.

The app itself is well under a megabyte; nothing in `docs/` reaches the bundle.

## By the numbers

Read at `2b6ff66`, `main` on 2026-09-25, the commit this page was rewritten from. Each
figure is the output of the command beside it at that commit; a later commit moves the
lines and the counts, not the commands. `tests/docs/accuracy.test.ts` asserts the shape
of this table — a command and a commit on every row — and does not recount the tree,
because a test that did would make this page wrong the moment the next task merged
(PLAN D-650).

| Figure | Value | Command | Read at |
|---|---|---|---|
| Tasks merged | 106 (R1–R102, H, P1, P2, P6) | `grep -c '^- \[x\] \*\*' TASKS.md` | `2b6ff66` |
| Tasks open | 3 (P3, P4, P5) | `grep -c '^- \[ \] \*\*' TASKS.md` | `2b6ff66` |
| Pull requests merged into `main` | 136 | `git log --oneline origin/main \| grep -cE '\(#[0-9]+\)$'` | `2b6ff66` |
| Commits on `main` | 232 | `git rev-list --count origin/main` | `2b6ff66` |
| Requirement ids named in the spec | 260 | `grep -oE '\bFR-[A-Z0-9]+-[0-9]+\b' SPEC.md \| sort -u \| wc -l` | `2b6ff66` |
| Highest decision id | `D-681` | `grep -oE '\bD-[0-9]+' PLAN.md \| sort -t- -k2 -n \| tail -1` | `2b6ff66` |
| Findings recorded (SPEC §4.20) | 100 (`F-1` … `F-100`) | `grep -cE '^\| \*?\*?F-[0-9]+' SPEC.md` | `2b6ff66` |
| Decision Log rows (SPEC §12) | 139 | `grep -cE '^\| [0-9]{4}-[0-9]{2}-[0-9]{2} \| V' SPEC.md` | `2b6ff66` |
| Lines in the three documents | 1850, 2435, 2748 | `wc -l SPEC.md PLAN.md TASKS.md` | `2b6ff66` |
| The driver | 396 lines, plus 7 files in `scripts/sdd/` | `wc -l scripts/sdd-run.ts; ls scripts/sdd \| wc -l` | `2b6ff66` |
| First commit of the driver | `6b79db1`, 2026-09-03 | `git log --diff-filter=A --format='%h %as' -- scripts/sdd-run.ts` | `2b6ff66` |
| Test files | 194 Vitest files, 83 Playwright specs | `git ls-files '*.test.ts' '*.test.tsx' \| wc -l; git ls-files 'tests/e2e/*.spec.ts' \| wc -l` | `2b6ff66` |
| Tests | 1791 in 194 files, the last two lines of its output | `npm test` | `2b6ff66` |
| Tracked files | 1703 | `git ls-files \| wc -l` | `2b6ff66` |
| Captures | 756 PNGs under `docs/screenshots/`, 254 of them the `v1-*` release set | `git ls-files 'docs/screenshots/*.png' \| wc -l; git ls-files 'docs/screenshots/v1-*.png' \| wc -l` | `2b6ff66` |
| Size of `docs/` | 101 MB, of which `docs/screenshots/` is 83 MB | `du -sm docs docs/screenshots` | `2b6ff66` |
